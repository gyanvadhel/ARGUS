streamlit>=1.33
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
plotly>=5.18

# Optional — only needed if you want the Call Assistant Demo to use a real LLM
# instead of its built-in scripted responder. Uncomment whichever you plan to use.
# anthropic>=0.30
# openai>=1.30
