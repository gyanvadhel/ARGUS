# Threat-intel sources (all free tiers). Missing keys just show "Not configured".
VIRUSTOTAL_API_KEY=
GOOGLE_SAFE_BROWSING_KEY=
ABUSECH_AUTH_KEY=
# Region used to read phone numbers without a +country code (US, IN, GB, ...)
ARGUS_DEFAULT_REGION=IN
